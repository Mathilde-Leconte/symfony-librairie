{% extends 'base-backend.html.twig' %}
{% block title %}
Nouvelle Home
{% endblock %}

{% block body %}
        <a href="{{ path('app_admin_home_index') }}" class="btn btn-dark btn-sm">Retour à la liste</a>
        <div class="d-flex justify-content-between align-items-center">
            <h1 class="mt-3">Nouvelle Home</h1>   
            {{ include('admin_home/_delete_form.html.twig') }}
        </div>
        {{ include('admin_home/_form.html.twig', {'button_label': 'Modifier'}) }}
{% endblock %}